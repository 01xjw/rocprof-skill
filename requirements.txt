# ROCprof Skill - Python Dependencies
# =====================================

# PyTorch with ROCm (install separately based on your ROCm version)
# See: https://pytorch.org/get-started/locally/
# Example: pip install torch --index-url https://download.pytorch.org/whl/rocm6.2

# Optional dependencies for analysis
numpy>=1.20.0

# Triton (for triton_runner.py)
# Install based on your ROCm version:
# pip install triton
triton>=2.1.0
